[project]
name = "api"
version = "0.1.0"
description = "API for ES"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "robyn>=0.84.0",
  "pydantic>=2.13.4",
  "pydantic-settings>=2.14.1",
  "devtools>=0.12.2",
]

[tool.uv.sources]
choco-coco-brain = { workspace = true }
choco-coco-image-to-ascii = { workspace = true }
